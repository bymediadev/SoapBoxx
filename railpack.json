{
  "$schema": "https://schema.railpack.com",
  "provider": "python",
  "steps": {
    "build": {
      "inputs": [{ "step": "install" }],
      "commands": []
    }
  },
  "deploy": {
    "preDeployCommand": "sh scripts/migrate_db.sh",
    "startCommand": "python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}
