# Network episode brief — v2 (compact / secondary)

**v3 is the primary episode workflow** (SoapBoxx Episode Report). This document names the **v2**
compact brief: JSON + one-page markdown from `backend/episode_intelligence.py`
(`REPORT_WORKFLOW_VERSION`). That format is still produced for compatibility and as the internal
base layer for v3.

The full spec for the **primary** coach report and workflow JSON is **`network_episode_brief_v3.md`**.
This file remains a stable alias for links that still say “v2”.
